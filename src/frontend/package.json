{
  "name": "medrequest-frontend",
  "version": "0.1.0",
  "description": "MedRequest — Patient communication frontend (demo/POC)",
  "private": true,
  "scripts": {
    "start": "npx http-server ./public -p 3000 -c-1 --cors -o",
    "dev": "npx http-server ./public -p 3000 -c-1 --cors"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
